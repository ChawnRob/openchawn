# Core runtime — Railway installs this file via Nixpacks (pip install -r requirements.txt).
fastapi
uvicorn
httpx
requests
python-dotenv
pydantic
psycopg[binary]>=3.1

# Optional (lazy import in app/memory/faiss_memory.py): faiss-cpu, numpy
# Not required for JSON/Postgres fractal memory or core chat. Install locally for vector index.
# Local tests: pip install -r requirements-dev.txt
