# OpenChawn

Official repository for OpenChawn.

## Memory Routing Source Of Truth

For V11.7, the source of truth for official chat memory routing is:

- `docs/OPENCHAWN_MEMORY_MAP_V11_7.md`

That document defines the official production memory path behind `POST /chat` and should be updated whenever the main chat memory routing changes.

